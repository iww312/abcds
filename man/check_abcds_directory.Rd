% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/directory.R
\name{check_abcds_directory}
\alias{check_abcds_directory}
\title{Check ABCDS Data Directory}
\usage{
check_abcds_directory(directory = NULL)
}
\arguments{
\item{directory}{Character string specifying the path to the ABCDS LONI data directory.
If \code{NULL} (default), retrieves the path from the \code{ABCDS_LONI_DATA_DIRECTORY}
environment variable.}
}
\value{
Invisibly returns the directory path if valid, or \code{NULL} if the directory
is not set, doesn't exist, or doesn't contain expected LONI files.
}
\description{
Verifies that the ABCDS LONI data directory exists and contains expected CSV files.
If the directory is not set, provides helpful instructions for setup. This function
is typically called automatically when the package loads.
}
\details{
The function performs the following checks:
\itemize{
\item If no directory is set, displays welcome message with setup instructions
\item If directory doesn't exist, warns user and suggests updating the path
\item If no expected LONI CSV files are found, alerts user to potential issues
}

Expected LONI files are defined in the internal \code{loni_file_names} object.
}
\examples{
\dontrun{
# Check if directory is properly configured
check_abcds_directory()

# Check a specific directory
check_abcds_directory("~/ABCDS/data")
}

}
\seealso{
\code{\link[=set_abcds_directory]{set_abcds_directory()}} to configure the data directory path
}
\keyword{internal}
